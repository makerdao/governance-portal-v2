type VoteTypes = 'Plurality Voting' | 'Ranked Choice IRV';

export default VoteTypes;

// TODO can we read the list from the type at runtime?
export const VoteTypesArray = ['Plurality Voting', 'Ranked Choice IRV'];
